package net.optifine.features.command.exception;

import net.optifine.util.ChatUtils;
import net.optifine.features.command.util.AbstractCommand;

public final class CommandError extends CommandException {
   public CommandError(String message) {
      super(message);
      "".length();
      if ("".length() != "".length()) {
         throw null;
      }
   }

   public void Oliver(AbstractCommand cmd) {
      ChatUtils.message(this.getMessage());
      "".length();
      if ((74 ^ 78) > 0) {
         ;
      }
   }

   static {
      "".length();
      if ("".length() == "".length()) {
         ;
      }
   }
}

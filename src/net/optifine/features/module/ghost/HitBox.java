package net.optifine.features.module.ghost;

import net.optifine.config.NumberSetting;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public class HitBox extends Module {
   // $FF: synthetic field
   private final NumberSetting Jack = new NumberSetting("Expand", 0.2D, 0.0D, 4.0D, 0.01D);

   public String Oliver() {
      String var1 = String.valueOf((new StringBuilder()).append(this.Jack.Jack()).append(""));
      "".length();
      return "   ".length() <= 0 ? null : var1;
   }

   static {
      "".length();
      if (null == null) {
         ;
      }
   }

   public HitBox() {
      super("HitBox", "Expands hitboxes.");
      this.Oliver(Category.Oscar);
      this.addSetting(this.Jack);
      "".length();
      if ("  ".length() <= 0) {
         throw null;
      }
   }

   public double Thomas() {
      double var10000;
      if (this.isEnabled()) {
         var10000 = this.Jack.Jack();
         "".length();
         if (-"   ".length() > 0) {
            return 0.0D;
         }
      } else {
         var10000 = 0.0D;
      }

      double var1 = var10000;
      "".length();
      return "   ".length() < 0 ? 0.0D : var1;
   }
}

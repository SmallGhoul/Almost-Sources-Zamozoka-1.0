package net.optifine.features.module.ghost;

import net.optifine.config.NumberSetting;
import net.optifine.config.NumberSetting.ValueDisplay;
import net.optifine.managers.event.events.EntityReachListener;
import net.optifine.managers.event.events.EntityReachListener.EntityReachEvent;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class Reach extends Module implements EntityReachListener {
   // $FF: synthetic field
   private final NumberSetting Jack;

   protected void Charlie() {
      EVENT_MANAGER.Jack(EntityReachListener.class, this);
      "".length();
      if (null == null) {
         ;
      }
   }

   public void Oliver(EntityReachEvent event) {
      event.Oliver = this.Jack.Jack();
      "".length();
      if (-(176 ^ 180) < 0) {
         ;
      }
   }

   public Reach() {
      super("Reach", "Extends your reach distance.\nMinecraft reach distance is 3.0 by default.", Category.Oscar);
      this.Jack = new NumberSetting("Reach", 3.2D, 1.0D, 6.0D, 0.01D, ValueDisplay.Oliver);
      this.Oliver(Category.Oscar);
      this.addSetting(this.Jack);
      "".length();
      if (-" ".length() > 0) {
         throw null;
      }
   }

   protected void Jacob() {
      EVENT_MANAGER.Oliver(EntityReachListener.class, this);
      "".length();
      if ((91 ^ 95) > " ".length()) {
         ;
      }
   }

   public double Thomas() {
      double var1 = (double)this.Jack.George();
      "".length();
      return "   ".length() < "".length() ? 0.0D : var1;
   }

   static {
      "".length();
      if (" ".length() == " ".length()) {
         ;
      }
   }

   public String Oliver() {
      String var1 = this.Jack.William();
      "".length();
      return "".length() != 0 ? null : var1;
   }
}

package net.optifine.features.module.combat;

import net.optifine.managers.event.events.DeathListener;
import net.optifine.util.gui.Translatable;
import net.minecraft.class_437;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class AutoRespawn extends Module implements DeathListener {
   public AutoRespawn() {
      super("AutoRespawn", Translatable.AUTO_RESPAWN, Category.Oliver);
      "".length();
      if ("   ".length() <= 0) {
         throw null;
      }
   }

   public void Thomas() {
      mc.field_1724.method_7331();
      mc.method_1507((class_437)null);
      "".length();
      if ("".length() == 0) {
         ;
      }
   }

   public void Jacob() {
      EVENT_MANAGER.Oliver(DeathListener.class, this);
      "".length();
      if ("".length() == 0) {
         ;
      }
   }

   static {
      "".length();
      if ((66 + 16 - 51 + 97 ^ 71 + 56 - 75 + 80) == (34 ^ 27 ^ 10 ^ 55)) {
         ;
      }
   }

   public void Charlie() {
      EVENT_MANAGER.Jack(DeathListener.class, this);
      "".length();
      if (null == null) {
         ;
      }
   }
}

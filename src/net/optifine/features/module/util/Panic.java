package net.optifine.features.module.util;

import net.optifine.Global;
import net.optifine.managers.event.events.UpdateListener;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

import java.util.Iterator;

public final class Panic extends Module implements UpdateListener {
   static {
      "".length();
      if ("".length() == 0) {
         ;
      }
   }

   public void Charlie() {
      EVENT_MANAGER.Jack(UpdateListener.class, this);
      "".length();
      if ("   ".length() == "   ".length()) {
         ;
      }
   }

   public Panic() {
      super("Panic", "Disables all modules");
      this.Oliver(Category.Charlie);
      "".length();
      if (-" ".length() > 0) {
         throw null;
      }
   }

   public void Jacob() {
      EVENT_MANAGER.Oliver(UpdateListener.class, this);
      "".length();
      if ("  ".length() < (114 + 99 - 205 + 160 ^ 6 + 115 - -10 + 41)) {
         ;
      }
   }

   public void Thomas() {
      Iterator var1 = Global.Jack().Oliver().iterator();

      do {
         if (!var1.hasNext()) {
            this.Jack(false);
            "".length();
            if ((29 ^ 8 ^ 113 ^ 96) <= "  ".length()) {
               return;
            }

            return;
         }

         Module module = (Module)var1.next();
         if (module.isEnabled() && module != this) {
            module.Jack(false);
         }

         "".length();
      } while((81 ^ 75 ^ 40 ^ 54) > "  ".length());

   }
}

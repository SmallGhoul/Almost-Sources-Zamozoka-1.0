package net.optifine.features.module.util;

import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class PortalGui extends Module {
   static {
      "".length();
      if ((252 ^ 167 ^ 9 ^ 86) >= 0) {
         ;
      }
   }

   public PortalGui() {
      super("PortalGUI", "Allows you to open inventory in portal.", Category.Charlie);
      "".length();
      if (-(154 ^ 158) >= 0) {
         throw null;
      }
   }
}

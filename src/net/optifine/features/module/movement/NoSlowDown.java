package net.optifine.features.module.movement;

import net.optifine.config.BooleanSetting;
import net.optifine.config.NumberSetting;
import net.optifine.config.NumberSetting.ValueDisplay;
import net.optifine.managers.event.events.StrafeEventListener;
import net.optifine.managers.event.events.StrafeEventListener.StrafeEvent;
import net.optifine.managers.event.events.UpdateListener;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class NoSlowDown extends Module implements StrafeEventListener, UpdateListener {
   // $FF: synthetic field
   private final BooleanSetting Harry;
   // $FF: synthetic field
   private final NumberSetting Jack;
   // $FF: synthetic field
   private final BooleanSetting Jacob;

   protected void Charlie() {
      EVENT_MANAGER.Jack(UpdateListener.class, this);
      EVENT_MANAGER.Jack(StrafeEventListener.class, this);
      "".length();
      if ((4 + 83 - -19 + 35 ^ 27 + 106 - 9 + 13) == (48 ^ 43 ^ 57 ^ 38)) {
         ;
      }
   }

   public boolean George() {
      byte var1 = this.Jacob.Jack();
      "".length();
      return (boolean)((92 ^ 99 ^ 17 ^ 42) != (90 + 102 - 114 + 110 ^ 40 + 44 - -42 + 58) ? "".length() : var1);
   }

   public void Thomas() {
      "".length();
      if ((37 ^ 33) == (181 ^ 177)) {
         ;
      }
   }

   public boolean Oscar() {
      byte var1 = this.Harry.Jack();
      "".length();
      return (boolean)(-" ".length() >= "".length() ? "".length() : var1);
   }

   static {
      "".length();
      if (" ".length() >= "".length()) {
         ;
      }
   }

   public String Oliver() {
      String var1 = this.Jack.William();
      "".length();
      return null != null ? null : var1;
   }

   public NoSlowDown() {
      super("NoSlowDown", "Allows you to ignore minecraft slowness factors", Category.Jack);
      this.Jack = new NumberSetting("Speed", 1.0D, 0.0D, 1.0D, 0.001D, ValueDisplay.Harry);
      this.Harry = new BooleanSetting("Remove Item Slowdown", true);
      this.Jacob = new BooleanSetting("Remove Block Slowdown", true);
      this.addSetting(this.Jack);
      this.addSetting(this.Harry);
      this.addSetting(this.Jacob);
      "".length();
      if (null != null) {
         throw null;
      }
   }

   public void Oliver(StrafeEvent event) {
      if (mc.field_1724.method_6115()) {
         event.Jack *= this.Jack.George();
      }

      "".length();
      if ("".length() >= 0) {
         ;
      }
   }

   protected void Jacob() {
      EVENT_MANAGER.Oliver(UpdateListener.class, this);
      EVENT_MANAGER.Oliver(StrafeEventListener.class, this);
      "".length();
      if (null == null) {
         ;
      }
   }
}

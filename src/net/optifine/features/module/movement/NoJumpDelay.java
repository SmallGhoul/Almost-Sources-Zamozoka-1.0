package net.optifine.features.module.movement;

import net.optifine.managers.event.events.PreMotionListener;
import net.optifine.mixinterface.ILivingEntity;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public class NoJumpDelay extends Module implements PreMotionListener {
   public NoJumpDelay() {
      super("NoJumpDelay", "Removes default jump delay.");
      this.Oliver(Category.Jack);
      "".length();
      if ("  ".length() <= " ".length()) {
         throw null;
      }
   }

   public void George() {
      ((ILivingEntity)IMC.getPlayer()).setJumpingCooldown(0);
      "".length();
      if ("   ".length() == "   ".length()) {
         ;
      }
   }

   protected void Jacob() {
      EVENT_MANAGER.Oliver(PreMotionListener.class, this);
      "".length();
      if ("".length() < (149 ^ 145)) {
         ;
      }
   }

   protected void Charlie() {
      EVENT_MANAGER.Jack(PreMotionListener.class, this);
      "".length();
      if ("   ".length() > 0) {
         ;
      }
   }

   static {
      "".length();
      if (-" ".length() <= 0) {
         ;
      }
   }
}

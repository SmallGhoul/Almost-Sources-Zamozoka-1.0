package net.optifine.features.module.movement;

import net.optifine.managers.event.events.UpdateListener;
import net.minecraft.class_243;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class NoWeb extends Module implements UpdateListener {
   static {
      "".length();
      if ("".length() <= 0) {
         ;
      }
   }

   public void Charlie() {
      EVENT_MANAGER.Jack(UpdateListener.class, this);
      "".length();
      if ("".length() <= (129 ^ 133)) {
         ;
      }
   }

   public void Thomas() {
      IMC.getPlayer().setMovementMultiplier(class_243.field_1353);
      "".length();
      if (-(20 ^ 43 ^ 155 ^ 161) < 0) {
         ;
      }
   }

   public NoWeb() {
      super("NoWeb", "Prevents you from getting slowed down by cobwebs.");
      this.Oliver(Category.Jack);
      "".length();
      if ((232 ^ 168 ^ 219 ^ 158) <= 0) {
         throw null;
      }
   }

   public void Jacob() {
      EVENT_MANAGER.Oliver(UpdateListener.class, this);
      "".length();
      if ((172 ^ 169) > 0) {
         ;
      }
   }
}

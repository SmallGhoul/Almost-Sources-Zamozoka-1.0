package net.optifine.features.module.movement;

import net.optifine.managers.event.events.UpdateListener;
import net.minecraft.class_746;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class Sprint extends Module implements UpdateListener {
   public void Jacob() {
      EVENT_MANAGER.Oliver(UpdateListener.class, this);
      "".length();
      if ("".length() == 0) {
         ;
      }
   }

   public void Charlie() {
      EVENT_MANAGER.Jack(UpdateListener.class, this);
      "".length();
      if (" ".length() != (104 ^ 74 ^ 103 ^ 65)) {
         ;
      }
   }

   public void Thomas() {
      class_746 player = mc.field_1724;
      if (!player.field_5976 && !player.method_5715()) {
         if (!player.method_5816() && !player.method_5869()) {
            if (lIIl(player.field_6250, 0.0F) > 0) {
               player.method_5728(true);
            }

            "".length();
            if ((12 ^ 8) < "  ".length()) {
               return;
            }
         } else {
            "".length();
            if (" ".length() == 0) {
               return;
            }
         }
      } else {
         "".length();
         if ("".length() < "".length()) {
            return;
         }
      }

   }

   public Sprint() {
      super("Sprint", "Makes you sprint all the time.");
      this.Oliver(Category.Jack);
      "".length();
      if (" ".length() != " ".length()) {
         throw null;
      }
   }

   static {
      "".length();
      if ("   ".length() != (94 ^ 47 ^ 2 ^ 119)) {
         ;
      }
   }

   private static int lIIl(float var0, float var1) {
      float var2;
      return (var2 = var0 - var1) == 0.0F ? 0 : (var2 < 0.0F ? -1 : 1);
   }
}

package net.optifine.features.module.movement;

import net.optifine.Global;
import net.optifine.managers.event.events.UpdateListener;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class SafeWalk extends Module implements UpdateListener {
   protected void Jacob() {
      EVENT_MANAGER.Oliver(UpdateListener.class, this);
      "".length();
      if (" ".length() > 0) {
         ;
      }
   }

   public SafeWalk() {
      super("SafeWalk", "Prevents you from falling down as if you were sneaking.");
      this.Oliver(Category.Jack);
      "".length();
      if ("   ".length() == "  ".length()) {
         throw null;
      }
   }

   static {
      "".length();
      if ((20 ^ 17) > 0) {
         ;
      }
   }

   public void Thomas() {
      Global.Thomas.Harry = true;
      "".length();
      if (null == null) {
         ;
      }
   }

   protected void Charlie() {
      EVENT_MANAGER.Jack(UpdateListener.class, this);
      Global.Thomas.Harry = false;
      "".length();
      if (" ".length() > -" ".length()) {
         ;
      }
   }
}

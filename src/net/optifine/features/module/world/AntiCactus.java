package net.optifine.features.module.world;

import net.optifine.managers.event.events.CactusCollisionShapeListener;
import net.optifine.managers.event.events.CactusCollisionShapeListener.CactusCollisionShapeEvent;
import net.minecraft.class_259;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class AntiCactus extends Module implements CactusCollisionShapeListener {
   protected void Jacob() {
      EVENT_MANAGER.Oliver(CactusCollisionShapeListener.class, this);
      "".length();
      if ("".length() == 0) {
         ;
      }
   }

   static {
      "".length();
      if ((108 ^ 5 ^ 196 ^ 169) > 0) {
         ;
      }
   }

   protected void Charlie() {
      EVENT_MANAGER.Jack(CactusCollisionShapeListener.class, this);
      "".length();
      if (-" ".length() <= 0) {
         ;
      }
   }

   public void Oliver(CactusCollisionShapeEvent event) {
      event.Oliver(class_259.method_1081(0.0D, 0.0D, 0.0D, 1.0D, 1.0000001D, 1.0D));
      "".length();
      if (null == null) {
         ;
      }
   }

   public AntiCactus() {
      super("AntiCactus", "Protects you from cactus damage.", Category.Jacob);
      "".length();
      if ("   ".length() == -" ".length()) {
         throw null;
      }
   }
}

package net.optifine.features.module.world;

import net.optifine.config.NumberSetting;
import net.optifine.config.NumberSetting.ValueDisplay;
import net.optifine.managers.event.events.UpdateListener;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class FastPlace extends Module implements UpdateListener {
   // $FF: synthetic field
   NumberSetting Jack;

   public void Charlie() {
      EVENT_MANAGER.Jack(UpdateListener.class, this);
      "".length();
      if ("  ".length() != "   ".length()) {
         ;
      }
   }

   public void Thomas() {
      if (IMC.getItemUseCooldown() > this.Jack.Oscar()) {
         IMC.setItemUseCooldown(this.Jack.Oscar());
      }

      "".length();
      if ("   ".length() > 0) {
         ;
      }
   }

   public String Oliver() {
      String var1 = String.valueOf((new StringBuilder()).append(this.Jack.Oscar()).append(""));
      "".length();
      return "   ".length() < 0 ? null : var1;
   }

   static {
      "".length();
      if ("   ".length() >= -" ".length()) {
         ;
      }
   }

   public FastPlace() {
      super("FastPlace", "Will place blocks faster", Category.Jacob);
      this.Jack = new NumberSetting("Cooldown", 0.0D, 0.0D, 20.0D, 1.0D, ValueDisplay.Oliver);
      this.addSetting(this.Jack);
      "".length();
      if ("   ".length() < " ".length()) {
         throw null;
      }
   }

   public void Jacob() {
      EVENT_MANAGER.Oliver(UpdateListener.class, this);
      "".length();
      if ("".length() == 0) {
         ;
      }
   }
}

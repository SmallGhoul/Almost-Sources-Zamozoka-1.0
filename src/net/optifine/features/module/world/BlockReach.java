package net.optifine.features.module.world;

import net.optifine.config.NumberSetting;
import net.optifine.managers.event.events.BlockReachListener;
import net.optifine.managers.event.events.BlockReachListener.BlockReachEvent;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public class BlockReach extends Module implements BlockReachListener {
   // $FF: synthetic field
   private final NumberSetting Jack = new NumberSetting("Reach", 4.5D, 1.0D, 9.0D, 0.01D);

   public void Oliver(BlockReachEvent event) {
      event.Oliver = this.Jack.George();
      "".length();
      if ("".length() == 0) {
         ;
      }
   }

   public String Oliver() {
      String var1 = String.valueOf((new StringBuilder()).append((double)Math.round(this.Jack.Jack() * 100.0D) / 100.0D).append(""));
      "".length();
      return -" ".length() > 0 ? null : var1;
   }

   public double Thomas() {
      double var1 = (double)this.Jack.George();
      "".length();
      return "".length() < -" ".length() ? 0.0D : var1;
   }

   protected void Jacob() {
      EVENT_MANAGER.Oliver(BlockReachListener.class, this);
      "".length();
      if (null == null) {
         ;
      }
   }

   protected void Charlie() {
      EVENT_MANAGER.Jack(BlockReachListener.class, this);
      "".length();
      if ((41 + 44 - 7 + 70 ^ 108 + 107 - 95 + 24) != "  ".length()) {
         ;
      }
   }

   public BlockReach() {
      super("BlockReach", "Modifies your block reach distance", Category.Jacob);
      this.addSetting(this.Jack);
      "".length();
      if ("  ".length() > "  ".length()) {
         throw null;
      }
   }

   static {
      "".length();
      if (null == null) {
         ;
      }
   }
}

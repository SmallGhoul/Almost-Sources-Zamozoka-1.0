package net.optifine.features.module.render;

import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class TrueSight extends Module {
   static {
      "".length();
      if ("  ".length() != 0) {
         ;
      }
   }

   public TrueSight() {
      super("TrueSight", "Allows to see invisible entities", Category.Harry);
      "".length();
      if ("  ".length() <= 0) {
         throw null;
      }
   }
}

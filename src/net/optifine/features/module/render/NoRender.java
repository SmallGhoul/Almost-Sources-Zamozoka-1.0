package net.optifine.features.module.render;

import net.optifine.config.BooleanSetting;
import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public class NoRender extends Module {
   // $FF: synthetic field
   private final BooleanSetting Thomas = new BooleanSetting("Disable Blindness", true);
   // $FF: synthetic field
   private final BooleanSetting Charlie = new BooleanSetting("Disable Hurt Animation", true);
   // $FF: synthetic field
   private final BooleanSetting George = new BooleanSetting("Disable Camera Wobble", true);
   // $FF: synthetic field
   private final BooleanSetting Jack = new BooleanSetting("Hide Pumpkin", true);
   // $FF: synthetic field
   private final BooleanSetting Harry = new BooleanSetting("Hide Fire", true);
   // $FF: synthetic field
   private final BooleanSetting Jacob = new BooleanSetting("Hide liquid overlay", true);

   public boolean Oscar() {
      byte var10000;
      if (this.isEnabled() && this.Jacob.Jack()) {
         var10000 = 1;
         "".length();
         if (null != null) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)(null != null ? "".length() : var1);
   }

   public boolean Thomas() {
      byte var10000;
      if (this.isEnabled() && this.Jack.Jack()) {
         var10000 = 1;
         "".length();
         if ("".length() != 0) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)("  ".length() == "   ".length() ? "".length() : var1);
   }

   public NoRender() {
      super("NoRender", "Removing some render features");
      this.Oliver(Category.Harry);
      this.addSetting(this.Jack);
      this.addSetting(this.Harry);
      this.addSetting(this.Jacob);
      this.addSetting(this.Charlie);
      this.addSetting(this.Thomas);
      this.addSetting(this.George);
      "".length();
      if (-"   ".length() >= 0) {
         throw null;
      }
   }

   public boolean Mary() {
      byte var10000;
      if (this.isEnabled() && this.George.Jack()) {
         var10000 = 1;
         "".length();
         if ((180 ^ 176) <= 0) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)(" ".length() >= "  ".length() ? "".length() : var1);
   }

   static {
      "".length();
      if (-"   ".length() <= 0) {
         ;
      }
   }

   public boolean William() {
      byte var10000;
      if (this.isEnabled() && this.Thomas.Jack()) {
         var10000 = 1;
         "".length();
         if ("".length() != 0) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)((116 ^ 114 ^ "  ".length()) < 0 ? "".length() : var1);
   }

   public boolean James() {
      byte var10000;
      if (this.isEnabled() && this.Charlie.Jack()) {
         var10000 = 1;
         "".length();
         if ("  ".length() <= 0) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)(-" ".length() > "".length() ? "".length() : var1);
   }

   public boolean George() {
      byte var10000;
      if (this.isEnabled() && this.Harry.Jack()) {
         var10000 = 1;
         "".length();
         if (-" ".length() == "".length()) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)("  ".length() <= -" ".length() ? "".length() : var1);
   }
}

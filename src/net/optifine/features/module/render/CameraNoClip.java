package net.optifine.features.module.render;

import net.optifine.features.module.Category;
import net.optifine.features.module.Module;

public final class CameraNoClip extends Module {
   public CameraNoClip() {
      super("CameraNoClip", "Your camera is ignoring blocks.", Category.Harry);
      "".length();
      if (null != null) {
         throw null;
      }
   }

   static {
      "".length();
      if ("".length() == 0) {
         ;
      }
   }
}

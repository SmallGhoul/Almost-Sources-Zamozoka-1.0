package net.optifine.clickgui;

import net.minecraft.class_4587;
import net.optifine.clickgui.window.AbstractWindow;

public abstract class PartOfGUI {
   // $FF: synthetic field
   private double Jacob;
   // $FF: synthetic field
   private AbstractWindow Charlie;
   // $FF: synthetic field
   private double Oliver;
   // $FF: synthetic field
   private double Jack;
   // $FF: synthetic field
   private double Harry;

   public void Harry(double width) {
      if (llIIllllIl(this.Harry, width) != 0) {
         this.George();
      }

      this.Harry = width;
      "".length();
      if ("   ".length() == "   ".length()) {
         ;
      }
   }

   public double Jack() {
      double var1 = this.Oliver;
      "".length();
      return "".length() > 0 ? 0.0D : var1;
   }

   static {
      "".length();
      if ("   ".length() > 0) {
         ;
      }
   }

   public void Oliver(AbstractWindow parent) {
      this.Charlie = parent;
      "".length();
      if (null == null) {
         ;
      }
   }

   public double Jacob() {
      double var1 = this.Harry;
      "".length();
      return (141 ^ 137) != (20 ^ 16) ? 0.0D : var1;
   }

   private static int llIIllllIl(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public void Jack(double y) {
      if (llIIllllII(this.Jack, y) != 0) {
         this.George();
      }

      this.Jack = y;
      "".length();
      if (null == null) {
         ;
      }
   }

   private void George() {
      if (this.Charlie != null) {
         this.Charlie.Oscar();
      }

      "".length();
      if ("  ".length() >= "".length()) {
         ;
      }
   }

   private static int llIIlllllI(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public double Harry() {
      double var1 = this.Jack;
      "".length();
      return (152 ^ 144 ^ 140 ^ 128) == "  ".length() ? 0.0D : var1;
   }

   private static int llIIlllIll(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public abstract void Oliver(class_4587 var1, double var2, double var4, float var6);

   public AbstractWindow Thomas() {
      AbstractWindow var1 = this.Charlie;
      "".length();
      return "  ".length() < "".length() ? null : var1;
   }

   public void Oliver(double x) {
      if (llIIlllIll(this.Oliver, x) != 0) {
         this.George();
      }

      this.Oliver = x;
      "".length();
      if (-" ".length() < 0) {
         ;
      }
   }

   public double Charlie() {
      double var1 = this.Jacob;
      "".length();
      return (84 ^ 80) <= 0 ? 0.0D : var1;
   }

   public void Jacob(double height) {
      if (llIIlllllI(this.Jacob, height) != 0) {
         this.George();
      }

      this.Jacob = height;
      "".length();
      if ("".length() <= (157 ^ 153)) {
         ;
      }
   }

   public PartOfGUI() {
      "".length();
      if (-"   ".length() >= 0) {
         throw null;
      }
   }

   public void Oliver(double mouseX, double mouseY, int mouseButton) {
      "".length();
      if ("   ".length() >= "   ".length()) {
         ;
      }
   }

   public abstract double Oliver();

   private static int llIIllllII(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }
}

package net.optifine.clickgui.window;

import net.minecraft.class_4587;
import net.optifine.clickgui.PartOfGUI;

import java.util.ArrayList;
import java.util.Iterator;

public abstract class AbstractWindow {
   // $FF: synthetic field
   private double Oscar;
   // $FF: synthetic field
   private double Mary;
   // $FF: synthetic field
   private boolean Elizabeth;
   // $FF: synthetic field
   private double Jennifer;
   // $FF: synthetic field
   protected boolean Jack;
   // $FF: synthetic field
   protected boolean Harry;
   // $FF: synthetic field
   private double George;
   // $FF: synthetic field
   protected double Charlie;
   // $FF: synthetic field
   private double William;
   // $FF: synthetic field
   private final String Thomas;
   // $FF: synthetic field
   public final ArrayList< PartOfGUI > Oliver = new ArrayList();
   // $FF: synthetic field
   protected boolean Jacob;
   // $FF: synthetic field
   private double Patricia;
   // $FF: synthetic field
   private double James;

   public final double Patricia() {
      double var1 = this.Patricia;
      "".length();
      return -(104 + 11 - 33 + 78 ^ 77 + 113 - 42 + 16) > 0 ? 0.0D : var1;
   }

   public final boolean Barbara() {
      byte var1 = this.Jacob;
      "".length();
      return (boolean)(-" ".length() != -" ".length() ? "".length() : var1);
   }

   public final double Jennifer() {
      double var1 = this.Jennifer;
      "".length();
      return "".length() != 0 ? 0.0D : var1;
   }

   public final void Jack(double y) {
      this.Oscar = y;
      "".length();
      if ((78 + 142 - 116 + 49 ^ 14 + 115 - 124 + 152) > " ".length()) {
         ;
      }
   }

   public final void George(double scrollVelocity) {
      this.Jennifer = scrollVelocity;
      "".length();
      if ("  ".length() == "  ".length()) {
         ;
      }
   }

   public void George() {
      if (this.Jack) {
         "".length();
         if (" ".length() > "  ".length()) {
            return;
         }
      } else {
         int offsetY = 4;
         double cWidth = this.James - 4.0D;
         Iterator var4 = this.Oliver.iterator();

         PartOfGUI c;
         while(var4.hasNext()) {
            c = ( PartOfGUI )var4.next();
            c.Oliver(2.0D);
            c.Jack((double)offsetY);
            c.Harry(cWidth);
            offsetY = (int)((double)offsetY + c.Charlie() + 4.0D);
            "".length();
            if ("".length() != "".length()) {
               return;
            }
         }

         this.Mary = (double)offsetY;
         boolean var10001;
         if (!this.Harry && llllIllll(this.Mary, this.William - 25.0D) > 0) {
            var10001 = true;
            "".length();
            if ("  ".length() < 0) {
               return;
            }
         } else {
            var10001 = false;
         }

         this.Elizabeth = var10001;
         if (this.Elizabeth) {
            this.Patricia = Math.min(this.Patricia, 0.0D);
            this.Patricia = Math.max(this.Patricia, -this.Mary + this.William - 25.0D);
         }

         var4 = this.Oliver.iterator();

         while(var4.hasNext()) {
            c = ( PartOfGUI )var4.next();
            c.Harry(cWidth);
            "".length();
            if ("".length() <= -" ".length()) {
               return;
            }
         }

         this.Jack = true;
         "".length();
         if ((39 ^ 35) < (90 ^ 94)) {
            return;
         }
      }

   }

   private static int lllllIIII(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   private static int lllllIIIl(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public final double Jack() {
      double var1 = this.George;
      "".length();
      return (163 ^ 189 ^ 85 ^ 79) < 0 ? 0.0D : var1;
   }

   public final double Harry() {
      double var1 = this.Oscar;
      "".length();
      return -"   ".length() > 0 ? 0.0D : var1;
   }

   private static int llllIlllI(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public double Mary() {
      double var1 = this.Charlie;
      "".length();
      return "   ".length() < " ".length() ? 0.0D : var1;
   }

   static {
      "".length();
      if ((132 ^ 128) != -" ".length()) {
         ;
      }
   }

   public final void Jacob(double height) {
      if (llllIllIl(this.William, height) != 0) {
         this.Oscar();
      }

      this.William = height;
      "".length();
      if ("  ".length() == "  ".length()) {
         ;
      }
   }

   public final void Oliver(double x) {
      this.George = x;
      "".length();
      if (-"   ".length() < 0) {
         ;
      }
   }

   private static int llllIllII(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public final void Thomas(double scrollOffset) {
      if (!this.Elizabeth) {
         "".length();
         if ("".length() == -" ".length()) {
            return;
         }
      } else {
         if (lllllIIIl(this.Patricia, scrollOffset) != 0) {
            this.Oscar();
         }

         this.Patricia = scrollOffset;
         "".length();
         if (null != null) {
            return;
         }
      }

   }

   public final double Charlie() {
      double var1 = this.William;
      "".length();
      return "".length() > 0 ? 0.0D : var1;
   }

   public final double Jacob() {
      double var1 = this.James;
      "".length();
      return "".length() >= "   ".length() ? 0.0D : var1;
   }

   public final void Charlie(double maxHeight) {
      if (lllllIIII(this.Charlie, maxHeight) != 0) {
         this.Oscar();
      }

      this.Charlie = maxHeight;
      "".length();
      if (-" ".length() <= -" ".length()) {
         ;
      }
   }

   public final double William() {
      double var1 = this.Mary;
      "".length();
      return "".length() >= (104 ^ 108) ? 0.0D : var1;
   }

   public final void Oliver( PartOfGUI partOfGUI ) {
      this.Oliver.add( partOfGUI );
      "".length();
      partOfGUI.Oliver(this);
      this.Oscar();
      "".length();
      if (-"   ".length() < 0) {
         ;
      }
   }

   public final String Oliver() {
      String var1 = this.Thomas;
      "".length();
      return -" ".length() > "  ".length() ? null : var1;
   }

   public final void Oscar() {
      this.Jack = false;
      "".length();
      if (-"   ".length() <= 0) {
         ;
      }
   }

   public final int James() {
      int var1 = this.Oliver.size();
      "".length();
      return (84 ^ 80) <= " ".length() ? "".length() : var1;
   }

   public final void Harry(double width) {
      if (llllIllII(this.James, width) != 0) {
         this.Oscar();
      }

      this.James = width;
      "".length();
      if (" ".length() < (62 ^ 58)) {
         ;
      }
   }

   private static int lllllIIlI(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public abstract void Oliver(class_4587 var1, int var2, int var3, float var4);

   private static int llllIllll(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public final boolean Linda() {
      byte var10000;
      if (lllllIIlI(this.Charlie(), 25.001D) <= 0 && this.Jacob) {
         var10000 = 1;
         "".length();
         if ("".length() == -" ".length()) {
            return (boolean)"".length();
         }
      } else {
         var10000 = 0;
      }

      byte var1 = var10000;
      "".length();
      return (boolean)((178 ^ 182) <= "   ".length() ? "".length() : var1);
   }

   public void Thomas() {
      double maxChildWidth = 0.0D;
      Iterator var3 = this.Oliver.iterator();

      while(var3.hasNext()) {
         PartOfGUI c = ( PartOfGUI )var3.next();
         if (llllIlllI(c.Oliver(), maxChildWidth) > 0) {
            maxChildWidth = c.Oliver();
         }

         "".length();
         if ((113 + 14 - 81 + 83 ^ 83 + 89 - 131 + 91) <= 0) {
            return;
         }
      }

      maxChildWidth += 4.0D;
      float titleBarWidth = Global.Harry.getGeneric().getWidth(this.Thomas) + 11.0F;
      double childrenHeight = 25.0D;
      Iterator var6 = this.Oliver.iterator();

      while(var6.hasNext()) {
         PartOfGUI c = ( PartOfGUI )var6.next();
         childrenHeight += c.Charlie() + 2.0D;
         "".length();
         if (-" ".length() > 0) {
            return;
         }
      }

      childrenHeight += 2.0D;
      if (llllIlllI(childrenHeight, this.Charlie + 25.0D) > 0 && llllIlllI(this.Charlie, 0.0D) > 0) {
         this.Harry(Math.max(maxChildWidth + 3.0D, (double)titleBarWidth));
         this.Jacob(this.Charlie + 25.0D);
         "".length();
         if (-"  ".length() >= 0) {
            return;
         }
      } else {
         this.Harry(Math.max(maxChildWidth, (double)titleBarWidth));
         this.Jacob(childrenHeight);
      }

      this.George();
      "".length();
      if ("  ".length() != (28 ^ 88 ^ 73 ^ 9)) {
         ;
      }
   }

   public final void Susan() {
      this.Jacob = true;
      "".length();
      if (-(178 ^ 182) <= 0) {
         ;
      }
   }

   public final boolean Elizabeth() {
      byte var1 = this.Elizabeth;
      "".length();
      return (boolean)((0 ^ 4) == -" ".length() ? "".length() : var1);
   }

   private static int llllIllIl(double var0, double var2) {
      double var4;
      return (var4 = var0 - var2) == 0.0D ? 0 : (var4 < 0.0D ? -1 : 1);
   }

   public final PartOfGUI Oliver( int index) {
      PartOfGUI var2 = ( PartOfGUI )this.Oliver.get(index);
      "".length();
      return " ".length() >= (97 ^ 19 ^ 66 ^ 52) ? null : var2;
   }

   public AbstractWindow(String title) {
      this.Thomas = title;
      "".length();
      if ("   ".length() > "   ".length()) {
         throw null;
      }
   }
}

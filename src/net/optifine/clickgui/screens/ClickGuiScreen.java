package net.optifine.clickgui.screens;

import net.minecraft.class_2585;
import net.minecraft.class_4587;
import net.optifine.clickgui.ClickGui;

public final class ClickGuiScreen extends BetterScreen {
   // $FF: synthetic field
   private final ClickGui Oliver;

   public boolean method_25421() {
      byte var1 = 0;
      "".length();
      return (boolean)(null != null ? "".length() : var1);
   }

   public boolean method_25401(double mouseX, double mouseY, double delta) {
      this.Oliver.Oliver(mouseX, mouseY, delta);
      byte var7 = super.method_25401(mouseX, mouseY, delta);
      "".length();
      return (boolean)("  ".length() > "  ".length() ? "".length() : var7);
   }

   public boolean method_25406(double mouseX, double mouseY, int mouseButton) {
      this.Oliver.Oliver(mouseX, mouseY, mouseButton);
      byte var6 = super.method_25406(mouseX, mouseY, mouseButton);
      "".length();
      return (boolean)((60 + 79 - 70 + 80 ^ 8 + 20 - -105 + 12) == -" ".length() ? "".length() : var6);
   }

   public void method_25419() {
      this.Oliver.Jack();
      "".length();
      if (-"  ".length() <= 0) {
         ;
      }
   }

   static {
      "".length();
      if (" ".length() != -" ".length()) {
         ;
      }
   }

   public void method_25394(class_4587 matrixStack, int mouseX, int mouseY, float partialTicks) {
      super.method_25394(matrixStack, mouseX, mouseY, partialTicks);
      this.Oliver.Oliver(matrixStack, mouseX, mouseY, partialTicks);
      "".length();
      if ("".length() <= (50 ^ 79 ^ 83 ^ 42)) {
         ;
      }
   }

   public boolean method_25402(double mouseX, double mouseY, int mouseButton) {
      this.Oliver.Oliver((int)mouseX, (int)mouseY, mouseButton);
      byte var6 = super.method_25402(mouseX, mouseY, mouseButton);
      "".length();
      return (boolean)(-" ".length() == "   ".length() ? "".length() : var6);
   }

   public ClickGuiScreen(ClickGui gui) {
      super(new class_2585(""));
      this.Oliver = gui;
      this.Oliver.Oliver(this);
      "".length();
      if ("  ".length() <= " ".length()) {
         throw null;
      }
   }
}
